================================================================================
眠音盒 · 第三方渠道换皮方案 · 产品分析
================================================================================
版本：v1.0
日期：2026-06
视角：产品经理
范围：同一微信小程序 AppID 下，为第三方提供差异化开屏/界面换皮；不涉及代开发多 AppID 方案详设。

--------------------------------------------------------------------------------
一、背景与诉求
--------------------------------------------------------------------------------

【业务背景】
  眠音盒核心能力（AI 助眠曲、贺卡、官方曲库等）已成型，希望将同一套小程序
  「授权/交付」给第三方合作方使用，对方在自有渠道推广时呈现对方品牌，
  我方保留统一研发与运维。

【核心诉求（来自业务方）】
  1. 不换小程序（同一 AppID、同一代码包、同一审核主体）。
  2. 我方用户：默认「眠音盒」开屏与视觉。
  3. 第三方用户：从专属入口进入时，看到第三方开屏与换皮。
  4. 关键追问：专属码进入后完全退出，再从微信「最近使用」进入，应显示哪套皮？

【非目标（本阶段不写实现细节）】
  - 每个第三方独立 AppID 的代开发全流程。
  - 微信小程序在微信搜一搜/列表中的名称、图标变更（属公众平台配置，非换皮范畴）。

--------------------------------------------------------------------------------
二、利益相关方与成功标准
--------------------------------------------------------------------------------

| 角色           | 关注点                         | 成功标准（可验收）                    |
|----------------|--------------------------------|---------------------------------------|
| 我方产品/运营  | 一套代码多客户、配置化上线     | 新渠道 1 天内可配置开屏+主题，无需发版 |
| 第三方合作方   | 品牌露出、用户「像自家产品」   | 专属码用户 ≥90% 看到己方品牌          |
| 终端用户       | 体验一致、不困惑               | 换皮后功能可用，无「假小程序」感        |
| 研发/运维      | 可维护、可回滚                 | 配置错误不影响默认渠道；有监控与开关    |
| 法务/合规      | 品牌授权、内容责任             | 合同明确素材权属；违规可下架渠道        |

--------------------------------------------------------------------------------
三、方案空间对比（是否必须换 AppID）
--------------------------------------------------------------------------------

| 方案 | 说明 | 同一 AppID 换皮 | 微信外壳名/图标 | 第三方「完全自有品牌」 | 研发成本 |
|------|------|-----------------|-----------------|------------------------|----------|
| A. 渠道换皮（推荐主方案） | channel + 远程 branding | ✅ | ❌ 仍显示眠音盒 | 部分满足 | 中 |
| B. 每客户独立小程序 | 各自注册 AppID，共用后端 | ✅ 可完全不同 | ✅ | ✅ | 高（N 套审核/发布） |
| C. 微信开放平台代开发 | 模板小程序 + 授权 ext 配置 | 模板一套 | ✅ 各商户独立 | ✅ | 高（需第三方平台资质） |

【结论】
  若合同仅要求「应用内开屏/配色/文案/模块」换皮，且接受微信列表仍显示「眠音盒」，
  → 选 **方案 A** 即可，与「不换小程序」一致。

  若合同要求应用商店级独立品牌（名称、图标、独立主体）→ 必须 B 或 C，已超出本分析主方案。

--------------------------------------------------------------------------------
四、用户进入路径与「认渠道」能力
--------------------------------------------------------------------------------

微信小程序冷启动时，是否携带渠道信息，取决于入口：

| 入口类型                     | 是否带 query（如 channel=xxx） | 典型场景           |
|------------------------------|--------------------------------|--------------------|
| 第三方专属小程序码/URL Link  | ✅ 可配置 path 参数            | 合作方 App、门店码 |
| 公众号菜单 / 模板消息链接    | ✅                             | 合作方私域         |
| 分享卡片（path 可带 channel）| ✅                             | 用户裂变           |
| 微信「最近使用」               | ❌ 一般无参数                  | 老用户回访         |
| 搜一搜 / 发现小程序          | ❌                             | 自然流量           |
| 从另一个小程序 navigateTo    | ⚠️ 取决于是否传 extraData      | 需约定             |

【产品含义】
  「仅当链接带 channel 才换皮」→ 从最近使用再进会回到默认皮，第三方体验断裂。
  「首次带 channel 进入后记住渠道」→ 最近使用再进仍保持第三方皮，符合多数 B 端预期。

--------------------------------------------------------------------------------
五、核心问题：专属码进入 → 退出 → 最近使用再进
--------------------------------------------------------------------------------

【用户路径】
  T0：扫 partner_a 专属码 → 开屏/首页为 A 品牌
  T1：完全退出小程序（划掉或多任务清除）
  T2：从「最近使用」再次打开

【三种产品策略】

  策略 1 · 仅链接生效（不持久化）
    T2 表现：默认「眠音盒」皮
    优点：逻辑简单；搜索/最近使用永远是官方品牌
    缺点：第三方用户第二次打开「变回眠音盒」，合作方难以接受
    适用：仅做一次性活动 landing，不要求品牌粘性

  策略 2 · 本地 Storage 持久化（推荐默认）
    首次 channel 写入 wx.setStorageSync('mb_channel', 'partner_a')
    T2 表现：仍为 A 品牌
    优点：无需登录即可记住；实现快；最近使用体验连续
    缺点：换机/删小程序/清缓存丢失；同机多渠道以最后一次为准
    适用：绝大多数第三方推广、门店码、私域链接

  策略 3 · 后端绑定（openid / userId）
    登录后将 channel 写入 users 表或 user_channel 表
    T2 表现：登录用户跨「最近使用」稳定；可跨设备（同微信）
    优点：可运营、可统计、可后台改绑；换机仍有效（登录后）
    缺点：未登录用户仍要依赖本地；需接口与隐私说明
    适用：需分渠道结算、CRM、或强制登录的业务

【推荐：策略 2 + 策略 3 组合（见第七节）】

--------------------------------------------------------------------------------
六、渠道信息存哪里？—— 本地 / 后端 / 远程配置 分工
--------------------------------------------------------------------------------

6.1 三类数据，不要混为一谈

  (1) 渠道身份 channelId        —— 「当前用户属于哪个合作方」
  (2) 渠道外观 branding 配置   —— 「该渠道长什么样、显哪些模块」
  (3) 渠道行为与统计           —— 「注册量、生成量、留存、结算」

6.2 仅本地 Storage

  存什么：
    mb_channel = 'partner_a'
    mb_channel_bound_at = 时间戳（可选）
    mb_branding_cache = { ... }  // 远程配置的本地缓存

  优点：无网络也能展示上次缓存的皮；冷启动快
  缺点：不可信（可被改）；无法后台强制换绑/下架；删小程序即丢

  结论：**适合作为「展示层缓存 + 未登录用户的渠道记忆」**，不能作为唯一真相源。

6.3 仅后端

  存什么（示例）：
    channels 表：id, name, status, contract_end, ...
    channel_branding 表：splash_title, logo_url, theme_json, features_json
    users.channel_id 或 user_channel_bindings 表

  优点：运营可配、可关停、可审计；统计准确；可 A/B
  缺点：首屏依赖网络（需缓存策略）；未登录用户无法绑定 openid

  结论：**branding 配置与结算数据必须以后端为准**；用户绑定视是否登录而定。

6.4 远程配置（CDN/API）

  GET /api/branding?channel=partner_a
  返回：开屏图、标题、slogan、主题色、功能开关、客服电话等

  与现有架构一致：
    - 已有 /api/promo/active 远程运营配置
    - 已有 wx-mini-apps 多端密钥
    - 已有 theme.js 双主题（晨雾/眠夜）可扩展为 channel 皮肤

  结论：**branding 内容放后端 + 小程序本地缓存（TTL 如 24h）**。

6.5 推荐数据流（摘要）

  冷启动
    → 读 query.channel（有则优先，可覆盖本地）
    → 无 query 则读 Storage mb_channel
    → 仍无则 channel = default
    → 拉取 /api/branding?channel=xxx（失败用本地缓存或 default 兜底）
    → 渲染 splash / 全局 theme
    → 若用户已登录：POST /api/user/channel-bind（异步，以后端为准）

--------------------------------------------------------------------------------
七、推荐产品方案（同一 AppID · 渠道换皮 v1）
--------------------------------------------------------------------------------

7.1 渠道生命周期

  【开通】运营在后台创建 channel，上传素材，生成专属小程序码（path 带 channel）
  【首次进入】带 channel 启动 → 写 Storage + 拉 branding → 可选登录后写后端
  【再次进入】最近使用 / 无参数 → 读 Storage（+ 登录用户读后端校验）
  【切换渠道】扫新 channel 码 → 新 channel 覆盖旧（需弹窗说明或静默，产品择一）
  【失效】后台禁用 channel → 下次拉 branding 返回 disabled → 回退 default 并清 Storage

7.2 渠道绑定规则（建议写进合作说明）

  规则 R1：query.channel 优先级高于 Storage（便于测试与活动强制切换）
  规则 R2：Storage 持久化，直到用户删小程序或运营禁用渠道
  规则 R3：已登录用户以后端绑定为准；本地与后端不一致时，以后端为准并回写 Storage
  规则 R4：default 渠道不写 Storage，或写 mb_channel=default
  规则 R5（可选）：渠道绑定后 N 天内不可被其他渠道覆盖（防恶意扫码劫持）

7.3 换皮范围（建议分期）

  P0（MVP，第三方最低可接受）
    - 开屏：背景、Logo、主标题、副标题、slogan
    - 全局主题色 / 导航栏 / Tab 栏（扩展 theme.js）
    - 关于页/联系客服：第三方电话、邮箱（已有 contact 页可配置化）

  P1（增强品牌感）
    - 首页 Banner、完成页文案
    - 按渠道隐藏：眠家商城、盒友圈、积分任务等（features 开关）
    - 运营弹窗 / promo 按 channel 过滤（扩展 /api/promo/active）

  P2（深度定制）
    - Tab 图标、自定义 Tab 数量（需评估 custom-tab-bar）
    - 分享卡片默认图/标题带渠道品牌
    - 贺卡水印、默认祝福语模板

  不可做（需独立 AppID）
    - 微信列表小程序名称、头像
    - 独立微信支付商户号（若第三方要自己收款，涉及商户号而非换皮）

7.4 与现有能力的关系

  现有                          渠道换皮后
  ----------------------------  ----------------------------------
  splash 固定「眠音盒」          读 branding.splash*
  theme dawn/night 用户自选      渠道默认主题 + 用户仍可选夜间模式（需定义优先级）
  mianjia-config 全局开关         per-channel features.hideMall
  promo-scheduler 全站活动        活动配置增加 channel 白名单/黑名单
  登录 wx-mini-apps 多端          不变；渠道与 AppID 解耦

  【主题优先级建议】
    用户手动切换 dawn/night > 渠道默认色 > 全局 default
    或：渠道合作禁止用户改主题（合同强品牌时）

--------------------------------------------------------------------------------
八、后端是否需要保存？—— 决策表
--------------------------------------------------------------------------------

| 数据项                     | 是否必须存后端 | 理由 |
|----------------------------|----------------|------|
| 渠道 branding 素材与开关   | ✅ 必须        | 运营配置、不发版、可关停 |
| 用户 ↔ channel 绑定        | ⚠️ 强烈建议    | 换机、统计、结算；登录用户必备 |
| 仅 Storage 的 channel      | ✅ 要有        | 未登录、首屏速度；但作为缓存 |
| 每次启动 channel 日志      | ⚠️ 建议        | 分析扫码转化率、异常渠道 |
| 用户生成作品/音频            | 已有 music 表  | 增加 channel_id 字段便于分账 |

【最小后端（MVP）】
  - channels + channel_branding 配置表
  - GET /api/branding?channel=
  - 管理端 CRUD + 生成小程序码 path 说明

【完整后端（推荐上线即做）】
  - 上述 +
  - PUT /api/user/channel-bind（登录后）
  - GET /api/user/me 返回 channelId
  - music_tracks / card_shares 等带 source_channel 字段（报表）
  - 渠道禁用 webhook 或定时任务（可选）

--------------------------------------------------------------------------------
九、典型用户故事与验收
--------------------------------------------------------------------------------

US-1 第三方门店顾客
  作为门店顾客，扫门店桌贴小程序码进入，看到门店品牌开屏；
  第二天从最近使用打开，仍是门店品牌；
  验收：Storage 有 channel；splash 文案/图为配置值；功能开关生效。

US-2 我方自然用户
  作为直接搜索「眠音盒」的用户，首次无 channel；
  验收：默认开屏；无第三方 Storage 污染。

US-3 运营下架违规渠道
  作为运营，后台禁用 partner_x；
  用户下次启动拉 branding 失败或 status=disabled；
  验收：回退 default，可选清 Storage 并 toast「服务已升级」。

US-4 同一用户扫两个渠道码
  先扫 A 再扫 B；
  验收：按 R1/R2 显示 B；后端绑定更新为 B；有审计日志。

US-5 登录用户换手机
  新手机首次登录同一微信；
  验收：后端 channel 下发，开屏为原绑定渠道（不依赖旧机 Storage）。

--------------------------------------------------------------------------------
十、风险、合规与运营
--------------------------------------------------------------------------------

10.1 产品风险

  - 品牌混淆：微信外壳仍是「眠音盒」，用户可能质疑「是不是山寨」
    → 合作合同与开屏脚注说明「技术服务由××提供」
  - 渠道劫持：恶意二维码写 channel=others
    → 渠道码由官方后台生成；可选绑定域名白名单；异常 channel 告警
  - 缓存陈旧：第三方更新了 Logo，用户仍看旧图
    → branding 版本号 + 强制刷新；或 TTL 24h
  - 与用户自选主题冲突
    → 见 7.4 优先级规则

10.2 合规

  - 第三方提供的 Logo/文案需授权书；涉医疗、睡眠功效表述需法务审
  - 隐私政策需说明：渠道来源、openid 与 channel 绑定用于服务与统计
  - 若第三方收集用户数据，需明确数据控制者（我方 vs 合作方）

10.3 运营流程（建议）

  1. 商务签约 → 分配 channelId
  2. 运营上传素材 → 预览链接（带 channel 的体验版二维码）
  3. 第三方验收 → 正式码交付
  4. 月度报表：该 channel 的 DAU、生成数、留存
  5. 合同到期 → 禁用 channel + 通知

--------------------------------------------------------------------------------
十一、技术实现要点（产品需知的边界，非开发详设）
--------------------------------------------------------------------------------

  - 入口页：保持 app.json 首屏 splash；onLoad 解析 channel
  - app.js onLaunch 与 splash 职责：统一 channel 解析函数，避免两处不一致
  - branding 缓存 key：mb_branding_{channel}_v{version}
  - 失败降级：网络失败 → 缓存 → default 硬编码
  - 包体积：Logo 走 HTTPS，不打进包
  - 审核：换皮素材变更若仅后端配置，通常不需重新提审；若改 WXML 结构需发版

  预估工作量（供排期参考，人天为粗估）：
    产品+设计：渠道字段定义、后台原型、合作 SOP     3～5
    前端：channel 解析 + splash/theme/features      5～8
    后端：branding API + 表结构 + 简单管理          5～8
    后端：user channel 绑定 + 报表字段               3～5
    测试：多入口、最近使用、清缓存、禁用渠道         3～4
    MVP 合计约 3～4 周（视管理后台是否从零搭建）

--------------------------------------------------------------------------------
十二、阶段路线图
--------------------------------------------------------------------------------

  Phase 0 · 决策确认（1 周）
    - 确认方案 A（同 AppID 渠道换皮）
    - 确认绑定策略：Storage + 登录写后端
    - 确认 P0 换皮范围与不可做项（微信名/图标）
    - 产出：渠道合作标准条款、验收用例

  Phase 1 · MVP
    - 后端 branding 配置 + GET 接口
    - splash 动态化 + 默认/渠道两套演示
    - Storage 持久化 + 最近使用验收通过
    - 1 个试点 partner 上线

  Phase 2 · 运营化
    - 管理后台配置渠道、生成 path 说明/码
    - 用户 channel 绑定、报表 source_channel
    - 按渠道隐藏商城/社区；promo 分渠道

  Phase 3 · 规模化（按需）
    - 多皮肤模板、预览工具
    - 渠道结算报表
    - 评估是否部分大客户需要独立 AppID（方案 B）

--------------------------------------------------------------------------------
十三、待产品决策项（开评审前需拍板）
--------------------------------------------------------------------------------

  D1：最近使用是否必须保持第三方皮？
      → 建议：是 → 采用 Storage 持久化（策略 2）

  D2：未登录用户是否足够，还是强制登录后才算「绑定渠道」？
      → 建议：未登录可 Storage；登录以后端为准（策略 2+3）

  D3：用户扫了新渠道码，是否静默覆盖旧渠道？
      → 建议：静默覆盖 + 关键页一次性 toast「欢迎使用××定制版」

  D4：是否允许用户在我方默认入口与第三方皮之间「手动切换」？
      → 建议：默认不允许；仅设置页 hidden 入口「恢复眠音盒官方版」（清 channel）

  D5：渠道 default 与「无 channel」是否同一套皮？
      → 建议：是

  D6：第三方是否必须隐藏眠家商城？
      → 建议：per-channel 配置，默认隐藏第三方渠道的商城入口

--------------------------------------------------------------------------------
十四、结论（产品经理摘要）
--------------------------------------------------------------------------------

  1. **不换小程序（同一 AppID）可以做第三方开屏与换皮**，通过 channel + 远程 branding
     实现；微信外的名称/图标不能改。

  2. **专属码进入后完全退出，再从「最近使用」进入**：
     - 若不做持久化 → 会变回眠音盒（一般不推荐对第三方）
     - **推荐**：首次带 channel 写入本地 Storage，最近使用仍读 Storage → **保持第三方皮**

  3. **后端必须参与**，但分工明确：
     - **后端存**：branding 配置、渠道启停、（登录用户）绑定关系、业务数据统计
     - **本地存**：当前 channel 缓存、branding 缓存（加速首屏）
     - **不能只靠本地**：无法运营关停、无法跨设备、无法结算

  4. **推荐方案**：方案 A 渠道换皮 + 策略 2（Storage）+ 策略 3（登录绑后端）+
     GET /api/branding + 可选 POST channel-bind；P0 从开屏与主题色起步。

  5. **下一步**：确认 D1～D6 → 输出渠道后台字段清单与 1 家试点 partner 验收标准 → 再进入研发排期。

--------------------------------------------------------------------------------
十五、可视化运营后台评估（查数据 · 导出报表 · 渠道配置）
--------------------------------------------------------------------------------

【评估问题】
  在渠道换皮方案之上，是否需要在现有后端之外，建设一套「可视化后台」？
  （Web 管理端：配渠道、看数据、导出 Excel，而非仅 secret 调 API）

15.1 现状：后端「有管理能力，无可视化界面」

  当前 music_sleep_api 已有若干「管理向」能力，但形态是 **HTTP API + 环境变量密钥**：

  | 能力               | 方式                              | 可视化 |
  |--------------------|-----------------------------------|--------|
  | 意见反馈列表       | GET /api/feedback?secret=         | ❌     |
  | 社区帖查询/删帖    | GET/DELETE /api/community/admin/* | ❌     |
  | 运营弹窗活动       | src/promo-data.js 代码 + 远程接口 | ❌（改配置需发版或改文件） |
  | 贺卡模板同步       | POST + x-sync-secret              | ❌     |
  | 业务日志           | app-YYYY-MM-DD.log 文件           | ❌（需 SSH/下载） |
  | 渠道 branding      | 尚未建设                          | —      |

  结论：**数据在库里有、能力可 API 化，但运营/商务/第三方无法自助操作**，
  渠道一多必然出现「改配置找研发、要报表导库」的瓶颈。

15.2 是否需要可视化后台？—— 结论先行

  | 阶段 / 条件                          | 是否需要独立可视化后台 |
  |--------------------------------------|----------------------|
  | 试点 1～2 家渠道、合同未要求数据报表 | **可先不做**，API + 脚本 + 日志 |
  | 渠道 ≥3 或需频繁改开屏/上下线        | **需要**（至少「渠道配置台」） |
  | 合同含分渠道结算 / 月度 KPI 交付     | **需要**（报表 + 导出） |
  | 第三方要求自助看自己的数据           | **需要**（Partner 门户或脱敏报表） |
  | 仅内部运营偶尔看总数                 | 可用 BI 工具连库，**不必自研全套** |

  **总建议：需要，但分阶段建设，不要 Phase 1 就做大而全中台。**

  - Phase 1（换皮 MVP）：**最小配置能力**（可简陋）+ **数据库埋点**
  - Phase 2（运营化）：**可视化渠道配置 + 内部报表 + CSV/Excel 导出**
  - Phase 3（规模化）：**Partner 自助门户、结算报表、权限与审计**

15.3 若不做可视化后台，会遇到什么（真实成本）

  1. 每次换 Logo/标题：改 JSON/DB 或找研发改 promo-data → 响应慢、易出错
  2. 渠道禁用/合同到期：无法自助一键关停，只能改 env 或停服
  3. 商务要「上月 A 渠道生成了多少首」：研发写 SQL 或 grep 日志 → 不可持续
  4. 第三方信任：无报表则合作难签 KPI 条款
  5. 合规审计：谁改了配置、何时下架，无界面则缺操作日志

  **试点期可忍 1～2 个月；进入正式商用渠道模式则不应长期依赖 secret API。**

15.4 可视化后台应服务谁？（角色与权限）

  | 角色           | 典型诉求                     | 后台模块               | 是否对外 |
  |----------------|------------------------------|------------------------|----------|
  | 我方超级管理员 | 建渠道、审素材、全局数据     | 渠道 CRUD、审计、总览  | 内部     |
  | 我方运营       | 改开屏、上下线活动、看 DAU   | Branding 编辑、报表    | 内部     |
  | 我方商务/财务  | 对账、导出、合同周期         | 结算报表、Excel 导出   | 内部     |
  | 第三方运营     | 只看本渠道数据、改本渠道素材 | Partner 视图（受限）   | 可选对外 |
  | 研发           | 不应再手工改配置             | API 即可，少登录后台   | —        |

  【产品原则】
    - 内部后台与「第三方门户」可同源不同权限，不必两套系统
    - 第三方默认 **只看本 channel**，不可见其他渠道与全站 PII

15.5 模块拆分与优先级（建议做什么、不做什么）

  ┌─────────────────────────────────────────────────────────────────┐
  │ M1 渠道与换皮配置（Phase 2 必做）                                │
  │   · 渠道列表：id、名称、状态、合同起止                           │
  │   · Branding 表单：开屏图/标题/slogan、主题色、功能开关           │
  │   · 预览：生成带 channel 的体验版路径说明 / 小程序码参数          │
  │   · 一键启用/禁用（对接 GET /api/branding 的 status）            │
  │   · 操作日志：谁何时改了什么                                     │
  └─────────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────────┐
  │ M2 数据看板（Phase 2 强烈建议）                                  │
  │   · 总览：按日 DAU、新增绑定用户、音乐生成数、贺卡数             │
  │   · 维度：channel、日期区间                                      │
  │   · 图表：趋势折线即可，不必先做复杂 BI                          │
  └─────────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────────┐
  │ M3 报表导出（Phase 2 有 KPI 合同则必做）                         │
  │   · 导出 CSV/Excel：渠道 × 日期 × 指标                           │
  │   · 指标建议：新增用户、活跃、生成完成数、分享数、登录数         │
  │   · 异步导出大表（可选），避免 HTTP 超时                         │
  └─────────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────────┐
  │ M4 现有能力迁入（Phase 2～3，降低 secret API 依赖）              │
  │   · 意见反馈列表页（已有 API）                                   │
  │   · 社区 moderation（已有 admin API）                            │
  │   · 运营弹窗 promo 配置（今天改 promo-data.js → 应进后台）       │
  └─────────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────────┐
  │ M5 Partner 门户（Phase 3，渠道多或合同要求时）                   │
  │   · 第三方账号登录，仅本 channel 报表 + 素材更新（可选审批流）   │
  └─────────────────────────────────────────────────────────────────┘

  【不建议 Phase 1～2 做的】
    - 完整低代码 BI、复杂拖拽大屏
    - 与渠道无关的全套 CMS
    - 自研权限系统超过「管理员 / 运营 / 只读 / partner」四档

15.6 建设路径对比（自研 vs 轻量 vs 混合）

  方案 W1 · 继续 secret API + 人工 SQL（仅试点）
    成本：低  |  适用：≤2 渠道、无 KPI 合同
    风险：不可扩展，第三方体验差

  方案 W2 · 自研轻量 Admin（推荐主路径）
    技术：Vue/React 单页 + 现有 Node 加 /api/admin/* + JWT/Session
    部署：子域名 admin.xxx.com 或 /admin 路径，内网/VPN 或 IP 白名单
    成本：中（约 2～4 人周起 M1+M2，视 UI 精细度）
    优点：与 branding、channel 模型一体；可渐进扩展 M3/M4
    缺点：需持续维护、需做登录与权限

  方案 W3 · 开源 Admin 框架（FastAdmin、react-admin、Retool 等）
    成本：中低～中  |  优点：CRUD 快  |  缺点：与现有栈贴合度需评估

  方案 W4 · BI 工具直连 SQLite（Metabase / Superset / 帆软）
    成本：低～中  |  仅解决 M2/M3「看数+导出」，**不解决 M1 配渠道**
    建议：**报表层可用 BI 补充，但不能替代渠道配置台**

  【推荐组合】
    **W2 自研轻量 Admin（M1 配置 + M2 看板 + M3 导出）**
    + 可选 W4 给财务做深度分析
    Phase 1 仅 W1 + **先把 source_channel 字段埋好**

15.7 报表指标与数据来源（产品要先定口径）

  需在业务表或事件表落库（渠道方案 Phase 1 就应埋点）：

  | 指标             | 口径建议                         | 数据来源                          |
  |------------------|----------------------------------|-----------------------------------|
  | 渠道绑定用户     | 首次带 channel 或登录 bind 成功  | user_channel_bindings             |
  | 日活 DAU         | 当日至少一次 API 带该 channel    | 启动日志 / daily_active 聚合表    |
  | 音乐生成完成     | status=completed 且 source_channel | music_tracks                    |
  | 贺卡创建         | card_shares 按 channel           | card_shares.source_channel        |
  | 封面上传失败率   | upload 502 等                    | 现有 HTTP 日志或 biz 事件         |

  **没有统一 channel 字段，后台做了也查不准** → 报表需求反向要求业务改造。

15.8 安全与合规（后台独有）

  - 管理员账号与小程序用户体系分离，禁止用 openid 当后台登录
  - 强密码 + 2FA（可选）+ 操作审计
  - Partner 门户脱敏：不导出 openid/手机号明文，或需额外授权
  - 素材上传：Logo 走现有 /api/upload/image，后台仅填 URL
  - 生产后台禁止公网裸奔：VPN、IP 白名单、或 SSO

15.9 工作量与路线图（叠加在原 Phase 上）

  | 阶段   | 小程序侧           | 后端 API              | 可视化后台              |
  |--------|--------------------|-----------------------|-------------------------|
  | Phase1 | 换皮 MVP           | branding GET + 埋点   | **不做**；secret 改 DB  |
  | Phase2 | features 开关      | admin CRUD + 聚合 API | **M1+M2+M3 最小版**     |
  | Phase3 | Partner 可选入口   | 结算逻辑              | M4 迁入 + M5 Partner 门户 |

  粗估增量（在原有 3～4 周换皮之上）：
    - M1 渠道配置台：5～8 人天
    - M2 看板（3～5 张图）：3～5 人天
    - M3 Excel 导出：2～3 人天
    - 登录权限审计：3～5 人天
    → Phase 2 后台合计约 **2～3 周**（1 全栈或 前端+后端）

15.10 待决策项（后台相关）

  D7：试点期是否接受「无后台、研发代配」？
      → 建议：仅 1 家试点可接受，签约第 2 家前必须上 M1

  D8：报表由我方单方提供，还是第三方也要登录查看？
      → 建议：合同写「我方每月提供 Excel」→ 先 M3；多客户同时要查 → 上 M5

  D9：运营弹窗 promo 是否纳入同一后台？
      → 建议：Phase 2 一并纳入，避免 promo-data.js 与 channel 两套配置

  D10：数据库 SQLite 是否满足报表并发？
      → 建议：渠道数少、日活万级内 SQLite + 夜间聚合够用；量大再迁 PG + BI

15.11 本节结论（产品经理摘要）

  1. **需要可视化后台，但不是换皮 MVP 的第一天就做**；试点可 secret+SQL，商用渠道模式必须做。
  2. **最优先**：M1 渠道/换皮配置台（否则运营绑在研发上）；**其次**：M2 看板 + M3 导出（KPI/结算）。
  3. **不能只做报表不做配置**：Metabase 等只解决看数，解决不了 Logo 上下线。
  4. **与渠道方案强绑定**：业务表加 source_channel、用户 channel 绑定表，Phase 1 就要埋点。
  5. **推荐路径**：Phase 2 自研轻量 Admin（W2）；财务深度分析可选 BI（W4）补充。

--------------------------------------------------------------------------------
十六、更新后的阶段路线图（含后台）
--------------------------------------------------------------------------------

  Phase 0 · 决策确认
    + 确认 D7～D10（后台范围、第三方是否自助看数）

  Phase 1 · 换皮 MVP（无可视化后台）
    - 小程序 channel + splash/theme
    - GET /api/branding；DB 埋 source_channel
    - 运营改配置：研发直连 DB 或临时 admin 脚本
    - 1 家试点

  Phase 2 · 运营化（**上可视化后台最小版**）
    - Admin M1：渠道 + branding CRUD、启停、审计
    - Admin M2/M3：按 channel 看板 + Excel 导出
    - 小程序 features 按 channel；promo 可迁后台（D9）

  Phase 3 · 规模化
    - Partner 门户（可选）
    - 结算报表、promo/反馈/社区统一运营台
    - 评估 SQLite → PG、独立 BI

================================================================================
文档结束
================================================================================
